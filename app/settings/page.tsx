import { PlaceholderPage } from "@/components/astra";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Preferences, data controls, integration readiness, and the quiet defaults that keep Astra calm."
    />
  );
}
